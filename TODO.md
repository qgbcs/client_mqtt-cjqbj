# TODO

- Replace the copied Xime input-method activity and application bootstrap.
- Add the Python client service and structured callback bridge.
- Add per-device request topic, key, timeout, and online polling settings.
- Add remote recursive scan pagination with path and symlink protection.
- Add target-side file/photo transfer RPC templates using `aliyun_git`.
- Add in-memory image preview and local file download.
- Add the unified second-level settings screen.
- Add focused Python tests and Android compile validation.
- Run device tests for two responders, pagination, file transfer, Wi-Fi, and both cameras.
